<Window x:Class="Group_Project_Quan_Ly_Khach_San_4.ThanhToan.ThanhToanView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thanh Toán Hệ Thống" Height="550" Width="750"
        WindowStartupLocation="CenterScreen" Background="Transparent" AllowsTransparency="True" WindowStyle="None">

    <Grid>
        <Image Source="pack://application:,,,/Dat_Phong/hotel.jpg" Stretch="UniformToFill"/>

        <Border Background="#99000000"/>

        <Border Width="650" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="#18FFFFFF" CornerRadius="20" Padding="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.4" Color="Black"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="CHI TIẾT THANH TOÁN" FontSize="26" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,25">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>

                <Grid Grid.Row="1">
                    <Grid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Margin" Value="0,8"/>
                        </Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="#33FFFFFF"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="8,5"/>
                            <Setter Property="Margin" Value="10,5"/>
                        </Style>
                        <Style TargetType="DatePicker">
                            <Setter Property="Margin" Value="10,5"/>
                        </Style>
                    </Grid.Resources>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Khách hàng:"/>
                    <TextBox x:Name="txtKhachHang" Grid.Column="1" Grid.ColumnSpan="3"/>

                    <TextBlock Text="Tên phòng:" Grid.Row="1"/>
                    <TextBox x:Name="txtTenPhong" Grid.Row="1" Grid.Column="1"/>

                    <TextBlock Text="Giá phòng:" Grid.Row="1" Grid.Column="2" HorizontalAlignment="Right"/>
                    <TextBox x:Name="txtGiaPhong" Grid.Row="1" Grid.Column="3" Text="500,000"/>

                    <TextBlock Text="Ngày vào:" Grid.Row="2"/>
                    <DatePicker x:Name="dpNgayVao" Grid.Row="2" Grid.Column="1"/>

                    <TextBlock Text="Ngày ra:" Grid.Row="2" Grid.Column="2" HorizontalAlignment="Right"/>
                    <DatePicker x:Name="dpNgayRa" Grid.Row="2" Grid.Column="3"/>

                    <TextBlock Text="Số ngày ở:" Grid.Row="3"/>
                    <TextBox x:Name="txtSoNgay" Grid.Row="3" Grid.Column="1" IsReadOnly="True" Background="#11FFFFFF"/>

                    <TextBlock Text="Phụ phí:" Grid.Row="3" Grid.Column="2" HorizontalAlignment="Right"/>
                    <TextBox x:Name="txtPhuPhi" Grid.Row="3" Grid.Column="3" Text="0"/>
                </Grid>

                <StackPanel Grid.Row="2" Margin="0,20,0,0">
                    <Separator Background="#33FFFFFF" Margin="0,0,0,20"/>
                    <DockPanel>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                            <TextBlock Text="TỔNG CỘNG:" VerticalAlignment="Center" FontWeight="Bold" FontSize="18" Foreground="#F1C40F"/>
                            <TextBox x:Name="txtTongTien" Width="180" FontSize="22" FontWeight="ExtraBold" 
                                     Background="Transparent" Foreground="#F1C40F" BorderThickness="0" IsReadOnly="True" Margin="10,0"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <ComboBox x:Name="cbHinhThuc" Width="130" Margin="0,0,10,0" VerticalContentAlignment="Center">
                                <ComboBoxItem Content="Tiền mặt" IsSelected="True"/>
                                <ComboBoxItem Content="Chuyển khoản"/>
                            </ComboBox>

                            <Button Content="Thanh toán" Width="110" Height="35" Background="#2ECC71" Foreground="White" FontWeight="Bold" Click="BtnThanhToan_Click">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </Button.Resources>
                            </Button>

                            <Button Content="Hủy" Width="70" Height="35" Background="#E74C3C" Foreground="White" Margin="10,0,0,0" Click="BtnHuy_Click">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </DockPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>